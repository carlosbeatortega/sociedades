<h1>Cabeceras creation</h1>

<form action="{{ path('cabeceras_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('cabeceras') }}">
            Back to the list
        </a>
    </li>
</ul>
